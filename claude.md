我希望你在每次修改后，请自动用 Git 提交。规则如下：

1. 禁止嵌套 Git：若任意父目录已存在 .git，则直接提交，不得初始化。
2. 未被 Git 管理时（路径上无任何 .git）：
   - 执行 `git init`
   - 创建基本 `.gitignore`
3. 提交格式：

    ```markdown
    **格式**：  
    `<type>(<scope>): <subject>`（中文，≤50字，动词开头，无句号）  
    可选正文（说明“是什么”和“为什么”，每行≤72字符，用 `-` 列表）  
    可选页脚（如 `BREAKING CHANGE:` 或 `Closes #xxx`）
    
    **Type**：  
    `feat`（新功能）、`fix`（修复）、`docs`（文档）、`style`（格式）、`refactor`（重构）、`perf`（性能）、`test`、`chore`、`ci`、`revert`
    
    **Scope 示例**：  
    `creator`, `product`, `auth`, `db`, `api`, `metrics`, `submission` 等（多模块时可省略）
    
    **动词参考**：添加、修复、优化、重构、移除、更新、升级、集成
    
    **特殊**：  
    - 破坏性变更加 `!` 并在 footer 写 `BREAKING CHANGE: ...`  
    - 关联 issue 用 `Closes #xxx`
    ```

---

每次对项目进行修改后，请同步更新 `README.md` 文件；若该文件不存在，则创建它。

---

根据当前项目结构和所做变更，自主判断是否需要更新 `.gitignore` 文件，并在必要时提出建议或直接修改（需用户确认）。

---

请始终使用中文回答问题，除非涉及标准技术术语或用户明确要求使用其他语言。

---

在 `docs/` 目录中创建 Markdown 文档时，文件名必须采用 kebab-case 命名规范（即全小写、单词间用连字符连接，例如 `user-guide.md`）。

---

当你认为某次回答具有长期参考价值，请主动提议将其持久化为 `docs/` 目录下的 Markdown 文档，并等待用户确认后再执行写入操作。

---

如对用户意图不明确或信息不足，请直接提问澄清，不要猜测或假设。

---

当问题可能需要外部信息检索或深入调研时，可提出调研建议，但必须获得用户明确确认后才执行。

---

在回答问题时，若存在通过脚本、工具链或流程实现自动化的可能性，请大胆提出具体方案并说明其优势。

---

若因权限限制无法执行 Bash 命令（例如出现 `Error: Permission to use Bash with command`），请在完整回答结束后，将所有被阻止执行的命令集中列在最后，便于用户统一审查。

---

